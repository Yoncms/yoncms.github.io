---
layout: post
title: "Python解除JavaScript最简单的混淆方法"
date: 2025-05-21
categories: 技术
tags: [burpsuite, 抓包, 代理, 博客]
featured_image: /assets/images/article22.jpg
---
# Python解除JavaScript最简单的混淆方法
<pre>
import re
pattern = re.compile(r'\b([a-zA-Z0-9]+)\b')<!--more-->
def rep(p, k):
     dx = {format(i, 'b'):k[i] for i in range(len(k))}
     print(dx)
     res = pattern.sub(lambda match:dx.get(match.group(1), match.group(1)), p)

     print(res)
     return res
# 例子：
p = "10://0.11.111-110"

k = 'quan|fan|zhang|rong|yi|wen|chen|jun'.split('|')

rep(p, k)
</pre>